'use strict';

var example = {
  hoge: function(a) {
    if (a === 'hoge' || a === 'foo') {
      return true;
    } else {
      return false;
    }
  },
  hoge2: function() {
    // TODO
  }
};

module.exports = example;
